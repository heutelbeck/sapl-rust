set "simpsons" first-applicable

policy "bart"
deny
	subject == "bs@simpsons.com" & action == "read"

policy "maggie"
permit
	subject == "ms@simpsons.com" & action == "read"
where
	time.secondOf(<time.now>) < 30;
